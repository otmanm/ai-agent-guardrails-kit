# Local benchmark results (what was actually run)

Generated by `benchmark/run-local.sh`. These are the cells that can be measured here without money, Docker, or large downloads.

- **Kit guard layer**: MEASURED. `tests/attack-matrix/run-matrix.sh` passes; see tests/attack-matrix/RESULTS.md for the per-path blocked/allowed table.
- **open-dynamic-workflows isolation (node:vm)**: MEASURED on node v22.22.3.
  ```
  MEASURED [open-dynamic-workflows isolation]: node:vm ESCAPED -> reached host fs (typeof result: string, value: Object)
  VERDICT: node:vm is NOT an isolation boundary. A workflow script can reach the real filesystem.
  ```

Everything not listed above (Claude Dynamic Workflows full pipeline, DannyMac180's playbook, OpenHands sandbox) is **not-runnable here**: it needs a paid LLM, Docker, or large downloads. See MATRIX.md for the sourced and inferred assessment of those, each with the exact command to run it yourself.
